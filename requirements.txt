# Development dependencies
-r requirements.txt

# PyTorch with CUDA (RECOMMENDED for speedups)
# Install CUDA version for best performance:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118  # CUDA 11.8 (RTX 30/40 series)
#   pip install torch --index-url https://download.pytorch.org/whl/cu121  # CUDA 12.1 (newer GPUs)
# CPU-only (no speedups - SpeedKit requires GPU Tensor Cores):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# Testing
pytest>=7.4

# Code quality
ruff>=0.6.0
mypy>=1.7

# Optional: Vision examples
torchvision>=0.15
